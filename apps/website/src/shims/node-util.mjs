// SPDX-License-Identifier: AGPL-3.0-only
/** Browser stand-in for the engine's proxy guard. Browser inputs are JSON data the page parsed itself. */
export const types = { isProxy: () => false };
export default { types };
