---
// SPDX-License-Identifier: AGPL-3.0-only
// The manual uses the one site header, unchanged: same lockup coordinate, same right-hand
// cluster, same global search. Starlight renders this inside its fixed <header>; the frame
// geometry that puts it on the shared page grid lives in styles/starlight.css.
import SiteHeader from '../Header.astro';
---
<SiteHeader surface="Docs" current="docs" />
